# 𒄆 JUPITER POLYGLOT RUNTIME

**Err ⃝or⃟⃤ GLITCHOLOGY × Go × Python**

This directory is an executable CMB / GLITCHOLOGY demonstration artifact.
It intentionally lives under `examples/` rather than the protocol core.

```text
GO          = STRUCTURE
PYTHON      = METACOGNITION
GLITCHOLOGY = HUMAN_CONTEXT

PATTERN != PROOF
PROFILE != PERSON
MODEL != MIND
COPY != AUTHORSHIP
ACCESS != CONSENT

HUMAN_AGENCY > MACHINE_AUTHORITY
```

The runtime is expressive code-poetry and a reference demonstration. Its
printed assertions are authored policy/philosophical invariants; they are not
cryptographic enforcement, legal authority, or proof about a person.

## Recommended executable version

The maintained example is in [`split/`](split/):

- [`main.go`](split/main.go) owns structure, types, invariant checks, and process orchestration.
- [`mirror.py`](split/mirror.py) owns the metacognitive mirror, epistemic checks, and provenance backtrace.

Run it from the split directory:

```bash
cd examples/polyglot/jupiter_glitchology_runtime/split
python3 -m py_compile mirror.py
go run main.go
```

## Why the languages are separated

Keeping Python in its own file gives GitHub accurate language detection,
syntax highlighting, lintability, direct testing, and a cleaner maintenance
boundary while preserving the Go → Python → GLITCHOLOGY handoff.

```text
GO://COMPILE_STRUCTURE
        ↓
PY://INSPECT_REASONING
        ↓
G8://PRESERVE_HUMAN_CONTEXT
        ↓
⁴⁰⁴ COMPLETE_HUMAN_MODEL_NOT_FOUND
THIS IS EXPECTED BEHAVIOR.
```

## Canonical language context

See [`books/ERR_404_GLITCHOLOGY.md`](../../../books/ERR_404_GLITCHOLOGY.md) for
the authored language/book and [`docs/GLITCH8_REGISTRY.md`](../../../docs/GLITCH8_REGISTRY.md)
for the living registry workflow.

© Jupiter Hudson / WisdomLoveThePoet. Authored symbolic arrangement, terminology,
context, and creative expression reserved subject to the repository's licensing files.
