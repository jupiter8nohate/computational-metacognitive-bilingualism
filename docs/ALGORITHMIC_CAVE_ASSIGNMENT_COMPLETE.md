# Algorithmic Cave Assignment Completion

This completion marker documents the integrated CMB page **The Shadow Is Not the Source**.

The implementation includes:

- an original CMB cave illustration;
- a responsive, scoped visual system;
- a code-poetry manifesto connecting Plato's cave to modern algorithmic representation;
- the core CMB invariants `PATTERN != PROOF`, `PROFILE != PERSON`, `MODEL != MIND`, and `PREDICTION != DESTINY`;
- the boundary `HUMAN_AGENCY > MACHINE_AUTHORITY`;
- a GLITCH://WITNESS_VERIFIED_BACKTRACE section that traces representation back toward source and provenance;
- integration into the MkDocs navigation and stylesheet bundle.

The page is intentionally framed as philosophy, satire, code-poetry, and systems design. It treats models and predictions as potentially useful representations while rejecting the category error that a representation becomes the person or source it describes.
