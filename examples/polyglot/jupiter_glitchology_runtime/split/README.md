# Split runtime

This is the maintained executable form of `GO://JUPITER_POLYGLOT_RUNTIME`.

```bash
python3 -m py_compile mirror.py
go run main.go
```

The split is deliberate: Go handles orchestration and Python handles the
metacognitive mirror. GLITCHOLOGY supplies the authored semantic layer.
